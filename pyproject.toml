[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "tei-pactols-enricher"
version = "0.1.0"
description = "Enrichissement déterministe de zones XML-TEI avec PACTOLS"
readme = "README.md"
requires-python = ">=3.11"
license = {text = "MIT"}
authors = [
  {name = "Nicolas Coquet"},
  {name = "Gaëlle David"},
]
dependencies = ["lxml>=5.0"]

[project.optional-dependencies]
dev = ["pytest>=8.0"]
web = ["fastapi>=0.116", "httpx>=0.28", "uvicorn[standard]>=0.35"]

[project.scripts]
pactols-enrich = "pactols_enricher.cli:main"
pactols-enrich-batch = "pactols_enricher.batch:main"

[tool.setuptools]
packages = ["pactols_enricher", "service"]
package-dir = {pactols_enricher = "src/pactols_enricher", service = "service"}

[tool.pytest.ini_options]
testpaths = ["tests"]
